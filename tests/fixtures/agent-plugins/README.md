Vendored from https://github.com/agentplugins/agent-plugins-spec (schemas/1.0.0),
so `tests/test_contract.py` can validate `plugin/plugin.json` and `plugin/mcp.json`
offline. Agent Plugins Specification 1.0.0 — licensed Apache-2.0 (schemas) /
CC-BY-4.0 (spec text); see the upstream LICENSE.md. Refresh when the pinned
`$schema` identifier in those manifests changes.
